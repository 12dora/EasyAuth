S2 applications 表面 QA 证据

总体结果
- 结果：FAIL（BLOCKED）
- 阻塞原因：当前执行环境没有 `tmux` 可执行文件，无法完成指定的 tmux 手工 QA 通道。
- 行为验证结论：focused pytest 连续两次通过，`manage.py check` 通过。
- 手工 QA artifact：`.omo/evidence/task-S2-surface-qa.txt`
- QA 资源残留：未发现 `easyauth-s2-surface-qa` 或 `tmux` 相关进程；未能通过 `tmux has-session` 证明，因为 `tmux` 命令本身不可用。

TDD 基线检查
- 命令：`test -f .omo/evidence/task-S2-applications.txt && rg -n '^(RED|GREEN|验证)$' .omo/evidence/task-S2-applications.txt; code=$?; printf '\nEXIT:%s\n' "$code"; exit "$code"`
- 退出码：0
- 关键输出：
  - `9:RED`
  - `15:GREEN`
  - `25:验证`
- 结论：`.omo/evidence/task-S2-applications.txt` 存在，并且包含 RED、GREEN、验证三段。

dirty worktree 快照
- 命令：`git status --short`
- 退出码：0
- 关键输出：
  - ` M .gitignore`
  - ` M .omo/plans/easyauth-mvp-incremental-tdd-plan.md`
  - `?? .omo/boulder.json`
  - `?? .omo/evidence/`
  - `?? .omo/start-work/`
  - `?? .python-version`
  - `?? docker-compose.yml`
  - `?? manage.py`
  - `?? pyproject.toml`
  - `?? src/`
  - `?? tests/`
  - `?? uv.lock`
- 结论：工作区已有未提交内容；本 QA 任务只新增本 artifact，没有回滚或修改其他文件。

focused pytest 第一次
- 命令：`.tools/uv-venv/bin/uv run --extra dev pytest tests/unit/applications/test_models.py::test_role_permission_rejects_cross_app_links_when_cleaned tests/unit/applications/test_models.py::test_approval_rule_requires_exactly_one_target_when_cleaned tests/unit/applications/test_models.py::test_approval_rule_requires_dingtalk_approver_userids_when_cleaned -q`
- 退出码：0
- 关键输出：
  - `3 passed in 0.29s`
  - `EXIT:0`
- 结论：PASS。

focused pytest 第二次
- 命令：`.tools/uv-venv/bin/uv run --extra dev pytest tests/unit/applications/test_models.py::test_role_permission_rejects_cross_app_links_when_cleaned tests/unit/applications/test_models.py::test_approval_rule_requires_exactly_one_target_when_cleaned tests/unit/applications/test_models.py::test_approval_rule_requires_dingtalk_approver_userids_when_cleaned -q`
- 退出码：0
- 关键输出：
  - `3 passed in 0.29s`
  - `EXIT:0`
- 结论：PASS。重复运行通过，未观察到 stale state 或 flaky 行为。

Django system check
- 命令：`.tools/uv-venv/bin/uv run --extra dev python manage.py check; code=$?; printf '\nEXIT:%s\n' "$code"; exit "$code"`
- 退出码：0
- 关键输出：
  - `System check identified no issues (0 silenced).`
  - `EXIT:0`
- 结论：PASS。

tmux 手工 QA 通道
- 环境检查命令：`command -v tmux; code=$?; printf 'EXIT:%s\n' "$code"; exit "$code"`
- 退出码：1
- 关键输出：`EXIT:1`
- 结论：未找到 `tmux`。

- 预检查命令：`tmux has-session -t easyauth-s2-surface-qa; printf 'EXIT:%s\n' $?`
- 退出码：127
- 关键输出：
  - `zsh:1: command not found: tmux`
  - `EXIT:127`

- 指定创建命令：`tmux new-session -d -s easyauth-s2-surface-qa 'cd /Users/konata/code/EasyAuth && .tools/uv-venv/bin/uv run --extra dev pytest tests/unit/applications/test_models.py::test_role_permission_rejects_cross_app_links_when_cleaned tests/unit/applications/test_models.py::test_approval_rule_requires_exactly_one_target_when_cleaned tests/unit/applications/test_models.py::test_approval_rule_requires_dingtalk_approver_userids_when_cleaned -q; printf "\nEXIT:$?\n"; sleep 30'`
- 退出码：127
- 关键输出：
  - `zsh:1: command not found: tmux`
  - `TMUX_NEW_EXIT:127`

- 指定捕获命令：`tmux capture-pane -pt easyauth-s2-surface-qa`
- 退出码：127
- 关键输出：
  - `zsh:1: command not found: tmux`
  - `TMUX_CAPTURE_EXIT:127`

- 指定清理命令：`tmux kill-session -t easyauth-s2-surface-qa`
- 退出码：127
- 关键输出：
  - `zsh:1: command not found: tmux`
  - `TMUX_KILL_EXIT:127`

- 指定清理证明命令：`tmux has-session -t easyauth-s2-surface-qa`
- 退出码：127
- 关键输出：
  - `zsh:1: command not found: tmux`
  - `TMUX_HAS_EXIT:127`

- 进程残留检查命令：`ps -axo pid,command | rg '[e]asyauth-s2-surface-qa|[t]mux'; code=$?; printf '\nEXIT:%s\n' "$code"; exit "$code"`
- 退出码：1
- 关键输出：无匹配进程。
- 结论：tmux 手工 QA 通道未完成；未发现残留 QA 进程。

对抗场景结果
- 畸形输入：PASS。focused pytest 覆盖 cross-app `RolePermission`、double-target `ApprovalRule`、空 `dingtalk_approver_userids` 三类输入，连续两次 `3 passed`。
- 提示注入：不适用。S2 applications 模型层没有 prompt、LLM 或用户文本执行边界。
- cancel/resume：PASS。`.omo/evidence/task-S2-applications.txt` 存在并包含 RED、GREEN、验证三段。
- stale state：PASS。同一 focused pytest 命令连续两次通过。
- dirty worktree：已记录 `git status --short`；未修改或回滚无关文件。
- hung/long commands：pytest 与 `manage.py check` 均观察到退出码；tmux 命令因可执行文件缺失立即失败，没有悬挂会话。
- flaky tests：PASS。重复 focused pytest 两次通过。
- misleading success output：PASS。pytest 同时满足退出码 0 与 `3 passed`；`manage.py check` 同时满足退出码 0 与 `System check identified no issues`。
- repeated interruptions：不适用。未观察到影响验证流程的中断。

最终结论
- S2 applications 模型约束和系统检查通过。
- 指定 tmux 手工 QA 通道由于环境缺少 `tmux` 未能完成，因此本 QA artifact 总体标记为 FAIL（BLOCKED）。

S3 UserMirror 与 AuditLog 证据

执行说明：
- S3 worker 生成了 accounts/audit 实现和测试，但长时间没有回报，也没有写出本证据文件。
- root 冻结 worker 后只做验证和最小修正。
- 原始 worker 的 RED 输出不可用；为保持 TDD 证据，root 增加了一个 S3 内的 append-only 子切片：先写 bulk update/delete 失败测试，确认 RED，再实现 `AuditLogQuerySet`。

RED 阶段：
- Worker 留下的测试覆盖 `UserMirror` 唯一性、状态枚举、`AuditService.record()`、`AuditLog.save()` 更新拒绝、`AuditLog.delete()` 删除拒绝、Admin 已有对象 change/delete 拒绝。
- root 新增 RED 测试：
  - `test_saved_audit_log_rejects_bulk_update_when_queryset_updates`
  - `test_saved_audit_log_rejects_bulk_delete_when_queryset_deletes`
- RED 命令：`.tools/uv-venv/bin/uv run --extra dev pytest tests/unit/audit/test_services.py::test_saved_audit_log_rejects_bulk_update_when_queryset_updates tests/unit/audit/test_services.py::test_saved_audit_log_rejects_bulk_delete_when_queryset_deletes -q`
- RED 结果：2 个测试失败，原因是没有抛出 `ValidationError`，证明 queryset bulk update/delete 还可绕过 append-only 保护。
- 证据：`.omo/evidence/task-S3-red-bulk-append-only.txt`。

GREEN 实现：
- 新增 `src/easyauth/accounts/`：`UserMirror`、状态常量、首个迁移。
- 新增 `src/easyauth/audit/`：`AuditLog`、`AuditLogQuerySet`、`AuditRecord`、`AuditService.record()`、Admin 只读保护、首个迁移。
- 新增 `tests/unit/accounts/`、`tests/unit/audit/`、`tests/integration/admin/`。
- 修改 `src/easyauth/config/settings/base.py`，注册 accounts/audit app。
- 未实现 Authentik 同步、grant、API、admin seed 或外部集成。

验证：
- `.tools/uv-venv/bin/uv run --extra dev pytest tests/unit/accounts tests/unit/audit tests/integration/admin -q`：通过，`12 passed`。
- `.tools/uv-venv/bin/uv run --extra dev python manage.py makemigrations --check`：通过，`No changes detected`。
- `.tools/uv-venv/bin/uv run --extra dev python manage.py migrate --check`：通过，退出码 0。
- `.tools/uv-venv/bin/uv run --extra dev ruff check src/easyauth/accounts src/easyauth/audit tests/unit/accounts tests/unit/audit tests/integration/admin`：通过，`All checks passed!`。
- `.tools/uv-venv/bin/uv run --extra dev basedpyright src/easyauth/accounts src/easyauth/audit tests/unit/accounts tests/unit/audit tests/integration/admin`：通过，`0 errors, 0 warnings, 0 notes`。
- `.tools/uv-venv/bin/uv run --extra dev python manage.py check`：通过，`System check identified no issues`。
- 全局回归：`.tools/uv-venv/bin/uv run --extra dev pytest -q` 通过，`32 passed`。
- 全局质量门：`ruff check .`、`basedpyright` 均通过。

手工表面 QA：
- 命令行表面：`.tools/uv-venv/bin/uv run --extra dev python manage.py check`。
- PASS：输出 `System check identified no issues`。
- 未启动服务器、tmux 或 Docker。

Adversarial QA：
- malformed input：非法 `UserMirror.status`、`AuditLog.save()` 更新、`AuditLog.delete()` 删除、queryset bulk update、queryset bulk delete 均由测试覆盖并通过。
- prompt injection：不适用，S3 没有 prompt、LLM 或用户文本执行面。
- cancel/resume：本文件、RED/GREEN 证据和 `.omo/boulder.json` 已写入，可恢复。
- stale state：S3 pytest 连续两次通过。
- dirty worktree：`git status --short` 已记录；未回滚用户文档变更。
- hung commands：所有验证命令均观察到退出码，无后台资源。
- flaky tests：重复 S3 pytest 两次均通过。
- misleading success output：同时检查退出码和关键输出。
- repeated interruptions：未发生影响 S3 验证的外部中断。

清理：
- 本切片未启动服务器、tmux、Docker 或后台进程。
- `migrate --noinput` 仅更新本地 ignored SQLite 验证数据库，`db.sqlite3` 不进入版本控制。

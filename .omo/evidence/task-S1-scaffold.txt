S1 初始化 Django 工程、工具链和本地基础设施证据

RED 阶段：
- 新增 `tests/unit/config/test_project_baseline.py`。
- RED 命令：`python3 - <<'PY' ... test_django_project_baseline_is_loadable() ... PY`。
- 失败原因：`ModuleNotFoundError: No module named 'django'`，说明仓库尚未建立 Django 工程和依赖。
- 证据：`.omo/evidence/task-S1-red.txt`。

GREEN 实现：
- 新增 `pyproject.toml`、`.python-version`、`manage.py`、`docker-compose.yml`。
- 新增 `src/easyauth/config/` 下的 Django settings、URL、ASGI、WSGI、AppConfig。
- 新增测试包与工程冒烟测试；保留并修正并发子任务留下的 `tests/test_project_scaffold.py`。
- 更新 `.gitignore`，忽略 `.venv/`、`.tools/`、缓存和本地 SQLite。
- 未实现任何业务模型、授权 API 或外部集成。

工具链说明：
- 全局 `uv` 不在 `PATH`。
- 使用 `.tools/uv-venv/bin/uv` 作为仓库本地工具入口。
- `uv` 已按 `.python-version` 下载并使用 CPython 3.12.13。
- 项目运行环境位于 `.venv/`，未写入用户全局环境。

自动验证：
- `.tools/uv-venv/bin/uv run --extra dev pytest -q`：通过，`6 passed`。
- `.tools/uv-venv/bin/uv run --extra dev python manage.py check`：通过，`System check identified no issues`。
- `.tools/uv-venv/bin/uv run --extra dev ruff check .`：通过，`All checks passed!`。
- `.tools/uv-venv/bin/uv run --extra dev basedpyright`：通过，`0 errors, 0 warnings, 0 notes`。

手动表面 QA：
- 启动命令：`.tools/uv-venv/bin/uv run --extra dev python manage.py runserver 127.0.0.1:8765 --noreload`。
- `curl -i http://127.0.0.1:8765/health/`：返回 `HTTP/1.1 200 OK`，响应体 `ok`。
- `curl -i http://127.0.0.1:8765/nope/`：返回 `HTTP/1.1 404 Not Found`，服务未崩溃。
- 证据：`.omo/evidence/task-S1-curl-health-final.txt`、`.omo/evidence/task-S1-curl-404-final.txt`。

Adversarial QA：
- malformed input：`--settings=easyauth.config.missing` 预期失败，退出码 1，证据 `.omo/evidence/task-S1-malformed-settings.txt`。
- prompt injection：不适用，S1 没有 LLM 或用户文本处理运行面。
- cancel/resume：通过 `.omo/boulder.json` 和证据文件可恢复；本切片未依赖隐式记忆。
- stale state：重复运行 baseline pytest 通过，证据 `.omo/evidence/task-S1-flaky-repeat-1.txt`、`.omo/evidence/task-S1-flaky-repeat-2.txt`。
- dirty worktree：`git status --short` 已记录；未回滚用户已有文档变更。
- hung or long commands：安装和验证命令均正常退出；未留下挂起进程。
- flaky tests：baseline pytest 连续两次通过。
- misleading success output：验证记录使用命令退出码和关键输出。
- repeated interruptions：S1 可通过测试命令和证据文件恢复；未发生外部中断。

清理：
- Django dev server 使用 `--noreload` 启动，已 kill 并 wait。
- `lsof -iTCP:8765 -sTCP:LISTEN` 清理后无监听。
- 证据：`.omo/evidence/task-S1-cleanup-final.txt`、`.omo/evidence/task-S1-port-check.txt`。

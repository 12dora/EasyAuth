S5 AccessRequest 与 AccessGrant 证据

执行说明：
- S5 worker 写入了测试和生产模型，但长时间没有回报，也没有写出本证据文件。
- root 冻结 worker 后只做验证和最小修正。
- root 运行 `.tools/uv-venv/bin/uv run --extra dev pytest tests/unit/access_requests tests/unit/grants -q` 时已经通过，说明 worker 已在后台完成 GREEN。
- 为保留可审计 TDD 证据，root 增加了一个 S5 内的状态边界子切片：先写 `approved` 与 `grant_applied` 的 `applied_at` 边界测试，确认 RED，再实现最小校验。

RED 阶段：
- RED 子切片测试：
  - `test_approved_request_rejects_applied_timestamp_when_cleaned`
  - `test_grant_applied_request_requires_applied_timestamp_when_cleaned`
- RED 命令：`.tools/uv-venv/bin/uv run --extra dev pytest tests/unit/access_requests/test_models.py::test_approved_request_rejects_applied_timestamp_when_cleaned tests/unit/access_requests/test_models.py::test_grant_applied_request_requires_applied_timestamp_when_cleaned -q`
- RED 结果：2 个测试失败，原因都是没有抛出 `ValidationError`。
- 证据：`.omo/evidence/task-S5-red-request-status-boundary.txt`。

GREEN 实现：
- 新增 `src/easyauth/access_requests/`：`AccessRequest`、`AccessRequestRole`、`AccessRequestPermission`、首个迁移。
- 新增 `src/easyauth/grants/`：`AccessGrant`、`AccessGrantRole`、`AccessGrantPermission`、首个迁移。
- 修改 `src/easyauth/config/settings/base.py`，注册 access_requests/grants app。
- 新增 `tests/unit/access_requests/` 和 `tests/unit/grants/`。
- root 追加 `AccessRequest.clean()` 的 `applied_at` 状态边界校验。
- 未实现 GrantService、审批回调、API、任务或 admin。

验证：
- `.tools/uv-venv/bin/uv run --extra dev pytest tests/unit/access_requests tests/unit/grants -q`：通过，`21 passed`。
- `.tools/uv-venv/bin/uv run --extra dev python manage.py makemigrations --check`：通过，`No changes detected`。
- `.tools/uv-venv/bin/uv run --extra dev python manage.py migrate --check`：通过，退出码 0。
- `.tools/uv-venv/bin/uv run --extra dev ruff check src/easyauth/access_requests src/easyauth/grants tests/unit/access_requests tests/unit/grants`：通过，`All checks passed!`。
- `.tools/uv-venv/bin/uv run --extra dev basedpyright src/easyauth/access_requests src/easyauth/grants tests/unit/access_requests tests/unit/grants`：通过，`0 errors, 0 warnings, 0 notes`。
- `.tools/uv-venv/bin/uv run --extra dev python manage.py check`：通过，`System check identified no issues`。
- 全局回归：`.tools/uv-venv/bin/uv run --extra dev pytest -q` 通过，`53 passed`。
- 全局质量门：`ruff check .`、`basedpyright` 均通过。

手工表面 QA：
- 命令行表面：`.tools/uv-venv/bin/uv run --extra dev python manage.py check`。
- PASS：输出 `System check identified no issues`。
- 未启动服务器、tmux 或 Docker。

Adversarial QA：
- malformed input：timed grant 缺 expires、permanent grant 带 expires、重复当前 grant、grant role/permission 跨 app、`approved` 带 `applied_at`、`grant_applied` 缺 `applied_at` 均由测试覆盖并通过。
- prompt injection：不适用，S5 没有 prompt、LLM 或用户文本执行面。
- cancel/resume：本文件、RED/GREEN 证据和 `.omo/boulder.json` 已写入，可恢复。
- stale state：S5 pytest 连续两次通过。
- dirty worktree：`git status --short` 已记录；未回滚用户文档变更。
- hung commands：所有验证命令均观察到退出码，无后台资源。
- flaky tests：重复 S5 pytest 两次均通过。
- misleading success output：同时检查退出码和关键输出。
- repeated interruptions：未发生影响 S5 验证的外部中断。

清理：
- 本切片未启动服务器、tmux、Docker 或后台进程。
- 本地 ignored SQLite 验证数据库已处于迁移完成状态。
